import { render, screen, theme } from 'utils/tests'
import {{pascalCase name}}, { {{pascalCase name}}Props } from './{{pascalCase name}}'

const init = (props?: {{pascalCase name}}Props) => {
  return render(<{{pascalCase name}} {...props} />)
}

describe('<{{pascalCase name}} />', () => {
  it('should render', () => {
    init()

    expect(sut).toBeTruthy
  })
})
