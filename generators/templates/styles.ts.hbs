import styled, { css } from 'styled-components'
import { {{pascalCase name}}Props } from './{{pascalCase name}}'

export const Wrapper = styled.div<{{pascalCase name}}Props>`
  ${({ theme }) => css`

  `}
`
